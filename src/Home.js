import Search from './Search';

const Home = () => {

    return (
        <div className="home">
            <Search />
            {/* { blogs && <BlogList blogs={ blogs } title="All Blogs" /> } */}
        </div>
    );
}
 
export default Home;